# docs/generated/ — regenerated-only artifacts

Artifacts in this directory are **produced by scripts, never by hand**. Hand-editing a
generated artifact is a lint error by decree of the genome (SEED.md §2) — the fix always
goes into the generator, then regenerate.

**Enforcement status:** not yet mechanical — the directory is empty and no generators
exist. This gap is priced as [E-001 in the entropy ledger](../plans/entropy-ledger.md);
the invariant (generation manifest + CI check) lands with the first generated artifact
(Stage 1 closed with none; the directory is still empty).

Every artifact added here must be registered in the generation manifest with its source
and regeneration command.
